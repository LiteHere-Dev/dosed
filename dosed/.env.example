# Copy to .env.local (or set in your shell / EAS build profile).
# EXPO_PUBLIC_ vars get inlined into the JS bundle at build time, so this
# is fine for a base URL but never put a real secret behind this prefix.
EXPO_PUBLIC_API_URL=https://your-deployed-server.example.com
